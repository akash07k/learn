# Your first task

What you'll be able to do: after this guide you will have completed the first concrete, verifiable
task in this subject, working entirely from the shell. Replace this placeholder with a real first
task that earns the reader an early, confidence-building win.

## Before you start

State the exact prerequisites: the tools that must be installed, the access the reader needs, and
any value they should have ready. Show the command that confirms each prerequisite is in place, so
the reader starts from a known-good footing.

## Do the task

Teach the task one step at a time. Above each code block, name the file being shown or edited, or
give a one-sentence lead-in for a command. Prefer non-interactive, shell-only forms over a
full-screen editor. Explain the "why" before each "do", and flag anything irreversible before the
command, not after.

```bash
# Replace this block with the real command for the task.
echo "hello, learn"
```

## Verify it worked

Give the exact command that confirms success and the key line to look for in its output, so the
reader can confirm the result from the shell without sighted help.

```bash
echo "hello, learn"
```

The expected output is the line `hello, learn`.

## Sources

- The repository-root `AUTHORING-CONVENTIONS.md` - the conventions this guide follows, including the
  "Verify it worked" requirement.

Previous: [00 -- Welcome](00-welcome.md)
